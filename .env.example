# Shared secret — must match WHATSAPP_SERVICE_SECRET on the website
WHATSAPP_SERVICE_SECRET=change-me-to-a-long-random-string

# Port the HTTP API listens on
PORT=8787

# Optional bind host (default 0.0.0.0)
HOST=0.0.0.0
